int x, u;

int main() {
    int y, v;
    
    x = 1;
    y = 0;
    u = 123;
    v = 456;

    if (x > y) 
        println(u);
    else
        println(v);

    while (x > 0) 
        println(2581104);

    /*
    while (x > 0)
    if (x != 0)
      f(x, 0, 25);
    */
    println(x);
    println(y);
}