int x, u;

int main() {
    x = 7;
    while (x > 1) {
        println(2581104);
        if (x > 9){
            x = 9;
        }
        else if (x > 8)
            x = 0;
        else
            return;
    }

    /*
    while (x > 0)
    if (x != 0)
      f(x, 0, 25);
    */
    println(x);
}